import { messages as portugueseMessages } from "./pt.ts";
import { messages as englishMessages } from "./en.ts";

const messages = {
  ...portugueseMessages,
  ...englishMessages,
};

export { messages };
